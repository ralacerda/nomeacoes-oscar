---
import BaseLayout from "./BaseLayout.astro";

export interface Props {
	title: string;
}

const { title } = Astro.props;
---

<BaseLayout title={`Sequela Movies | ${title}`}>
	<main>
	<slot />
	</main>
</BaseLayout>
